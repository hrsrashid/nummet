module BodeShooting where

import           Function
import           Library

compute :: (Double, Vector, FMatrix, Matrix, Matrix) -> Either ComputeError Matrix
compute (tau, domain, afs, ps, qs) = undefined
